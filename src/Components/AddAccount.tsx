import styled from "styled-components";

export const AddAccount = () => {
  return (
    <>
      <h3>
        You don't have accounts... <a href="/">click here to add an account</a>
      </h3>
    </>
  );
};
